/*
 * Dashboard Username Admin Sorting
 * Copy/Pasted from List.js Example, modified
 * Thx for awesome free code!
 */
//= require list.min.js

    // Define value names
    var options = {
        valueNames: [ 'id', 'name', 'email', 'role' ]
    };

    // Init list
    var contactList = new List('contacts', options);

    var idField = $('#id-field'),
        nameField = $('#name-field'),
        emailField = $('#email-field'),
        roleField = $('#role-field'),
        addBtn = $('#add-btn'),
        editBtn = $('#edit-btn').hide(),
        removeBtns = $('.remove-item-btn'),
        editBtns = $('.edit-item-btn');

    // Sets callbacks to the buttons in the list
    refreshCallbacks();

    function refreshCallbacks() {
        // Needed to add new buttons to jQuery-extended object
        removeBtns = $(removeBtns.selector);
        editBtns = $(editBtns.selector);

        removeBtns.click(function() {
           var itemId = $(this).closest('tr').find('.id').text();
           contactList.remove('id', itemId);
        });

        editBtns.click(function() {
           var itemId = $(this).closest('tr').find('.id').text();
           var itemValues = contactList.get('id', itemId).values();
           idField.val(itemValues.id);
           nameField.val(itemValues.name);
           ageField.val(itemValues.age);
           cityField.val(itemValues.city);

           editBtn.show();
           addBtn.hide();
        });
    }

    function clearFields() {
       nameField.val('');
       emailField.val('');
       roleField.val('');
    }

/*
 * Dashboard Username Admin Sorting
 * Copy/Pasted from List.js Example, modified
 * Thx for awesome free code!
 */
//= require list.min.js

    // Define value names
    var venueOptions = {
        valueNames: [ 'venue-id', 'city', 'country', 'region' ]
    };

    // Init list
    var venueList = new List('venues', venueOptions);




$('#admin-user-link').click(function() {
  $('#admin-user-link').slideUp('slow', function() {
    $('#admin-user-section').slideDown('slow', function() {
    // Animation complete.
     });
  });
});


$('#admin-venue-link').click(function() {
  $('#admin-venue-link').slideUp('slow', function() {
    $('#admin-venue-section').slideDown('slow', function() {
    // Animation complete.
     });
  });
});

$('#admin-game-link').click(function() {
  $('#admin-game-link').slideUp('slow', function() {
    $('#admin-game-section').slideDown('slow', function() {
    // Animation complete.
     });
  });
});

$('#admin-user-link-close').click(function() {
  $('#admin-user-section').slideUp('slow', function() {
    $('#admin-user-link').slideDown('slow', function() {
    // Animation complete.
     });
  });
});


$('#admin-venue-link-close').click(function() {
  $('#admin-venue-section').slideUp('slow', function() {
    $('#admin-venue-link').slideDown('slow', function() {
    // Animation complete.
     });
  });
});

$('#admin-game-link-close').click(function() {
  $('#admin-game-section').slideUp('slow', function() {
    $('#admin-game-link').slideDown('slow', function() {
    // Animation complete.
     });
  });
});