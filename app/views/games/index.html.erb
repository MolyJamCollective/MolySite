<h1>Listing games</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Inspiration</th>
    <th>Event</th>
    <th>Venue</th>
    <th>Group</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @games.each do |game| %>
  <tr>
    <td><%= game.name %></td>
    <td><%= game.inspiration %></td>
    <td><%= game.event_id %></td>
    <td><%= game.venue_id %></td>
    <td><%= game.group_id %></td>
    <td><%= link_to 'Show', game %></td>
    <td><%= link_to 'Edit', edit_game_path(game) %></td>
    <td><%= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Game', new_game_path %>
