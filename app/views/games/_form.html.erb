<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :inspiration %><br />
    <%= f.text_area :inspiration %>
  </div>
  <div class="field">
    <%= f.label :event %><br />
    <%= f.collection_select(:event_id, Event.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :venue %><br />
    <%= f.collection_select(:venue_id, Venue.all, :id, :display_name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
