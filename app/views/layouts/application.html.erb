<!DOCTYPE html>
<html>
<head>
  <title>MolySite</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= csrf_meta_tags %>
  <% if content_for?(:meta) %><%= yield(:meta) %><% end %>
  <% if content_for?(:css) %><%= yield(:css) %><% end %>
  
</head>
<body>
  <header class="header">
      <div class="header-container">
        <a class="brand" href="/"><%= image_tag("header-logo.png",:class => "logo") %><!-- img src="assets/img/header-logo.png" class="logo" --></a>
        <nav class="main">
          <ul class="menu"> 
            <li class="button"><button class="menu">Menu</button></li>
			 <%# TODO: I'm pretty sure these links should be generated by rails %>
            <li class="link"><a href="/venues">Venues</a></li>
          </ul>
       </nav>
	   <%# Wait to implement this once everything else works <ul> %>
			 
	     <%# render 'layouts/registration' %>
	     <%# render 'layouts/login' %>
			 <%# </ul> %>
      </div>
</header><!--/.header -->
<div class="page">
  <!-- Begging of in Page elements -->
  <section class="content">
    <% flash.each do |type, message|
      case type 
        when :alert %><div class="alert alert-error"><strong>Alert!</strong><% 
        when :notice %><div class="alert alert-success"><strong>Notice!</strong><% 
        when :message %><div class="alert alert-info"><strong>Message!</strong><% 
      end %><button type="button" class="close" data-dismiss="alert">&times;</button> <%= message %></div>
    <% end %>
    <% yield %>
    <%if content_for?(:masthead) %><div class="masthead">
      <%= yield(:masthead)%>
    </div><!-- end .masthead --><% end %>
  
    <%if content_for?(:leftColumn) %><div class="left-column">
      <%= yield(:leftColumn)%>
    </div><!-- end .left-column --><% end %>
  
    <%if content_for?(:rightColumn) %><aside class="right-column">
      <%= yield(:rightColumn)%>
    </aside><!-- end .right-column --><% end %>
  
  </section><!-- /.content -->
</div><!-- /.page -->
<footer class="footer">
      This is the footer area, where there will be some text and maybe a link block. It's outside the main container, though.
</footer>
	<%= javascript_include_tag "application" %>
  <%= javascript_include_tag "header" %>
  <% if content_for?(:javascript) %><%= yield(:javascript) %><% end %>
</body>
</html>
