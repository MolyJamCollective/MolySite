source 'https://rubygems.org'

gem 'rails', '3.2.12'
gem 'sqlite3'

group :assets do
  gem 'uglifier', '>= 1.0.3'
end

# CSS Normalize
gem 'normalize-rails'
gem 'jquery-rails'

group :development do
  # Color Syntax in IRB & Rails Console
	gem 'wirble'

  # Built in Debugging
	gem 'better_errors'		
	gem 'binding_of_caller'

  # Rails Panel
	gem 'meta_request'		
end

# User Auth
gem 'devise'
gem 'cancan'

# Geolocation
gem 'geocoder'

# Markdown
gem 'redcarpet'

# Gravatar
gem 'gravtastic'
